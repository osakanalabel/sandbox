# プロジェクトルール

## Webアプリ作成ルール
- HTMLワンファイルのWebアプリを作成する
- ファイル名は `index.html` 以外にする
- JS・CSSはHTMLファイル内に含める（外部ファイル不可）
- 作成後、リポジトリルートの `index.html` に公開URLのリンクを追加する
- mainブランチに直接コミット・プッシュしてよい（即公開）

## 進め方
- 指示から公開までのスピードを最優先
- 細かい判断はClaudeに委ねる（ユーザーへの確認を最小限にする）
